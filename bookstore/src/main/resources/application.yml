spring:
  application:
    name: bookstore

server:
  port: 8080

# Phase 1: only the health endpoint is exposed. More endpoints arrive in later phases.
management:
  endpoints:
    web:
      exposure:
        include: health

logging:
  level:
    root: INFO
    com.bookstore: INFO
